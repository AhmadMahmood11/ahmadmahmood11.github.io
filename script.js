const menuBtn=document.getElementById('menuBtn');const navMenu=document.getElementById('navMenu');menuBtn.addEventListener('click',()=>navMenu.classList.toggle('open'));document.getElementById('year').textContent=new Date().getFullYear();
const glow=document.querySelector('.cursor-glow');window.addEventListener('mousemove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});
const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')})},{threshold:.12});reveals.forEach(el=>observer.observe(el));
const track=document.getElementById('sliderTrack');const slides=[...document.querySelectorAll('.project-card')];const dotsWrap=document.getElementById('dots');let index=0;slides.forEach((_,i)=>{const b=document.createElement('button');b.className='dot'+(i===0?' active':'');b.setAttribute('aria-label','Go to project '+(i+1));b.onclick=()=>goTo(i);dotsWrap.appendChild(b)});const dots=[...document.querySelectorAll('.dot')];function updateSlider(){track.style.transform=`translateX(-${index*100}%)`;dots.forEach((d,i)=>d.classList.toggle('active',i===index))}function goTo(i){index=i;updateSlider()}document.getElementById('nextBtn').onclick=()=>{index=(index+1)%slides.length;updateSlider()};document.getElementById('prevBtn').onclick=()=>{index=(index-1+slides.length)%slides.length;updateSlider()};setInterval(()=>{index=(index+1)%slides.length;updateSlider()},5500);
const canvas=document.getElementById('neuralCanvas');const ctx=canvas.getContext('2d');let w,h,particles;function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;particles=Array.from({length:Math.min(95,Math.floor(w*h/18000))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.55,vy:(Math.random()-.5)*.55,r:Math.random()*1.8+1}))}resize();addEventListener('resize',resize);function draw(){ctx.clearRect(0,0,w,h);for(const p of particles){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(24,232,255,.72)';ctx.fill()}for(let i=0;i<particles.length;i++){for(let j=i+1;j<particles.length;j++){const a=particles[i],b=particles[j];const dx=a.x-b.x,dy=a.y-b.y,dist=Math.sqrt(dx*dx+dy*dy);if(dist<145){ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle=`rgba(141,92,255,${1-dist/145})`;ctx.lineWidth=.7;ctx.stroke()}}}requestAnimationFrame(draw)}draw();
