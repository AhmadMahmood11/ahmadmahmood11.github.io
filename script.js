const canvas=document.getElementById('neuralCanvas'),ctx=canvas.getContext('2d');let w,h,pts=[];function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;pts=Array.from({length:Math.min(110,Math.floor(w*h/15000))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.45,vy:(Math.random()-.5)*.45}))}addEventListener('resize',resize);resize();let mx=w/2,my=h/2;addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY});function draw(){ctx.clearRect(0,0,w,h);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.fillStyle='rgba(33,232,255,.55)';ctx.beginPath();ctx.arc(p.x,p.y,1.7,0,Math.PI*2);ctx.fill()});for(let i=0;i<pts.length;i++){for(let j=i+1;j<pts.length;j++){let a=pts[i],b=pts[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<145){ctx.strokeStyle=`rgba(141,92,255,${(145-d)/900})`;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}let d2=Math.hypot(pts[i].x-mx,pts[i].y-my);if(d2<210){ctx.strokeStyle=`rgba(28,255,195,${(210-d2)/520})`;ctx.beginPath();ctx.moveTo(pts[i].x,pts[i].y);ctx.lineTo(mx,my);ctx.stroke()}}requestAnimationFrame(draw)}draw();
const reveal=()=>document.querySelectorAll('.reveal').forEach(el=>{if(el.getBoundingClientRect().top<innerHeight*.86)el.classList.add('show')});addEventListener('scroll',reveal);reveal();
const slides=[...document.querySelectorAll('.project')],pager=document.querySelector('.pager');let current=0;slides.forEach((_,i)=>{let b=document.createElement('button');b.onclick=()=>go(i);pager.appendChild(b)});function go(i){slides[current].classList.remove('active');current=(i+slides.length)%slides.length;slides[current].classList.add('active');[...pager.children].forEach((b,k)=>b.classList.toggle('active',k===current))}document.querySelector('.next').onclick=()=>go(current+1);document.querySelector('.prev').onclick=()=>go(current-1);go(0);setInterval(()=>go(current+1),5200);
